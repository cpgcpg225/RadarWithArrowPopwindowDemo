<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <declare-styleable name="ArrowTextView">
        <attr name="arrowOrientation" format="dimension">
            <enum name="left" value="1" />
            <enum name="top" value="2" />
            <enum name="right" value="3" />
            <enum name="bottom" value="4" />
        </attr>
        <attr name="arrowWidth" format="dimension" />
        <attr name="arrowHeight" format="dimension" />
        <attr name="arrowLocation" format="float" />
        <attr name="contentRadius" format="dimension" />
        <attr name="backgroundColor" format="color" />
        <attr name="arrowOffsetX" format="dimension" />
        <attr name="arrowOffsetY" format="dimension" />
    </declare-styleable>
</resources>
